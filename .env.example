# Server-only secrets. Never prefix these with NEXT_PUBLIC_.
OPENAI_API_KEY=
ELEVENLABS_API_KEY=

# Optional: override the default realtime voice model.
OPENAI_REALTIME_MODEL=gpt-realtime-mini

# Only needed when developing the disabled Convex storage prototype.
NEXT_PUBLIC_CONVEX_URL=
CONVEX_DEPLOYMENT=
